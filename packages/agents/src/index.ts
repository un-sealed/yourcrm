/**
 * `@yourcrm/agents` — autonomous agent runtime boundary (Phase 3).
 *
 * INTENTIONAL PLACEHOLDER. Agent runs are approval-gated
 * (`ai.action_requested` -> `ai.action_approved` -> execute) and consume
 * domain services, never repositories.
 */

export const AGENTS_BOUNDARY_VERSION = 0 as const
