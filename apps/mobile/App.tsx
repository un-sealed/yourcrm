// Expo / React Native entrypoint (Phase 4 — native mobile/offline).
// Intentional placeholder: reserves apps/mobile so deep links, shared
// validation schemas and the API contract stay compatible. No scripts yet;
// excluded from turbo pipelines until the Phase 4 agent scaffolds Expo.
export default function App(): null {
  return null
}
